# 星際防衛前線 - 敵人角色數值與圖檔來源指南

這份文件彙整了《星際防衛前線》（Star Defense Frontline）中所有敵人角色的各項能力數值、詳細的數值計算公式、圖檔來源與繪製方式，以及各自的特殊能力機制。

---

## 1. 核心數值計算公式與機制

遊戲中敵人的屬性會隨著波數（Wave）以及玩家擊殺魔王（Bosses Killed）的數量而進行動態成長。

### 1.1 生命值 (HP) 計算公式
- **一般小怪 / 魔王（正常計算）**：
  $$\text{最大生命值} = \lfloor \text{BASE\_ENEMY\_HP} \times \text{hpMod} \times \text{waveMult} \times \text{bossScale} + \text{bonusHp} \rfloor$$
  - $\text{BASE\_ENEMY\_HP} = 40$
  - $\text{waveMult} = 1 + (\text{Wave} \times 0.25)$
  - $\text{bossScale} = 2^{\lfloor \text{G.gameState.bossesKilled} / 10 \rfloor}$ （每擊殺 10 個 Boss，所有敵人的生命值翻倍成長）
  - 對於小怪群 (`group === 'minion'`)：
    $\text{bonusHp} = (\text{Wave} - 1) \times 5$ （包含每波基本小怪生命值成長常數 `MINION_HP_PER_WAVE = 5`，所有小兵的 `hpPerWave` 皆為 0）
  - 對於魔王級 (`group === 'boss'`)：
    $\text{bonusHp} = 0$
- **固定生命值覆寫（不隨波數成長）**：
  - **母艦魔王 (`boss_carrier`)**：最大生命值固定為 $1000$。
  - **特攻骷髏劍 (`skullsword`)**：最大生命值固定為 $100$。

### 1.2 移動速度 (Speed) 計算公式
- **移動速度**：
  $$\text{速度} = (\text{BASE\_ENEMY\_SPEED} \times \text{speedMod} \times \text{DRAW\_SCALE}) + \min(2.0, \text{Wave} \times 0.05)$$
  - $\text{BASE\_ENEMY\_SPEED} = 1.5$
  - $\text{DRAW\_SCALE} = 1.0$
  - 移動速度會隨波數以每次 $+0.05$ 的速度增加，波數速度加成上限為 $2.0$。
  - **狂暴魔王 (`boss_berserker`) 特殊機制**：當生命值低於 50% 時，速度隨生命值降低而提高：
    $$\text{目前速度} = \text{基礎速度} \times (1 + (0.5 - \text{HP比例}) \times 2)$$
    在極限瀕死狀態下，速度最高可達基礎速度的 2 倍。

### 1.3 擊殺資金獎勵 (Reward) 計算公式
- **非魔王敵人**：
  $$\text{獎勵} = \max\left(1, \lfloor (10 + \text{Wave} \times 1.5) \times \sqrt{\text{bossScale}} \times \text{無人機係數} \rfloor\right)$$
  - 若為 `drone` 則「無人機係數」為 `0.2`，其他皆為 `1`。
  - 特例：`skullsword` 的獎勵固定為 `0`。
- **魔王級敵人 (Boss)**：
  $$\text{獎勵} = \text{非魔王獎勵} \times 5$$ （即乘上 `BOSS_REWARD_MULTIPLIER = 5`）

### 1.4 到達基地造成的基地重創 (Base Damage)
當敵人成功突破防線到達終點時，扣除的基地 HP：
- **魔王級 (Boss)**：扣除 $500$ HP（並觸發系統警告）。
- **坦克級 (`tank`, `tankbug`)**：扣除 $100$ HP。
- **特攻無人機 (`skullsword`)**：撞擊基地時扣除其**當前剩餘生命值 (`hp`)**。
- **普通小怪級 (其他)**：扣除 $50$ HP。

---

## 2. 敵人角色數值一覽表

以下數值以 **第 1 波 (Wave = 1)**，且 **未擊殺任何魔王 (bossesKilled = 0)** 作為基準計算：

| 敵人代號 (Key) | 角色名稱 | 分組 (Group) | 生命倍率 (hpMod) | 速度倍率 (speedMod) | 基準生命值 (Wave 1) | 基準速度 (Wave 1) | 碰撞半徑 (radius) | 基準獎勵 (Wave 1) | 基地傷害 | 主動攻擊力 | 攻擊射程 | 攻擊冷卻 |
| :--- | :--- | :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| `normal` | 普通兵 | 小兵 (Minion) | 1.0 | 1.0 | 50 | 1.55 | 12 | 11 | 50 | - | - | - |
| `scout` | 偵察兵 | 小兵 (Minion) | 0.5 | 1.8 | 25 | 2.75 | 9 | 11 | 50 | - | - | - |
| `tank` | 坦克 | 小兵 (Minion) | 2.5 | 0.6 | 125 | 0.95 | 16 | 11 | 100 | - | - | - |
| `healer` | 治療兵 | 小兵 (Minion) | 1.2 | 0.8 | 60 | 1.25 | 14 | 11 | 50 | - | - | - |
| `stealth` | 隱形兵 | 小兵 (Minion) | 0.8 | 1.2 | 40 | 1.85 | 11 | 11 | 50 | - | - | - |
| `drone` | 雄蜂無人機 | 小兵 (Minion) | 0.3 | 2.2 | 15 | 3.35 | 6 | 2 | 50 | - | - | - |
| `boss_berserker`| 狂暴魔王 | 魔王 (Boss) | 7.0 | 0.6 | 350 | 0.95 | 22 | 55 | 500 | 30 | 180 | 40 幀 |
| `boss_carrier` | 母艦魔王 | 魔王 (Boss) | 9.0 | 0.4 | 1000 (固定) | 0.65 | 26 | 55 | 500 | - | - | - |
| `skullsword` | 特攻骷髏劍 | 小兵 (Minion) | 1.0 | 3.0 | 100 (固定) | 4.55 | 16 | 0 | 當前HP | 自爆撞擊 | 自爆撞擊 | - |
| `boss_armored` | 裝甲魔王 | 魔王 (Boss) | 12.0 | 0.4 | 600 | 0.65 | 24 | 55 | 500 | 40 | 200 | 60 幀 |
| `boss_teleporter`| 傳送魔王 | 魔王 (Boss) | 6.0 | 0.5 | 300 | 0.80 | 20 | 55 | 500 | 15 | 250 | 35 幀 |
| `tankbug` | 坦克蟲 | 小兵 (Minion) | 5.0 | 0.55 | 250 | 0.875 | 24 | 11 | 100 | 150 | 150 | 60 幀 |

*註：攻擊力與冷卻的「幀」以遊戲主循環 60 FPS 計算。例如 60 幀代表每 1 秒攻擊一次。*

---

## 3. 敵人圖檔來源與繪製方式

在遊戲中，敵人的圖檔來源主要分為：**精靈圖 (Sprite Sheet)**、**專屬獨立 PNG** 與 **動態 SVG** 三種形式。

| 敵人代號 (Key) | 角色名稱 | 圖檔來源 / 繪製方式 | 精靈圖行列或渲染細節 |
| :--- | :--- | :--- | :--- |
| `normal` | 普通兵 | 精靈圖 `PIC/EEMYAs.png` | 第一排 (Row 0)，單幀尺寸 32x32px。支援方向旋轉及 4 幀行走動畫。備用為動態 SVG。 |
| `scout` | 偵察兵 | 精靈圖 `PIC/EEMYAs.png` | 第二排 (Row 1)，單幀尺寸 32x32px。支援方向旋轉及 4 幀行走動畫。備用為動態 SVG。 |
| `tank` | 坦克 | 精靈圖 `PIC/EEMYAs.png` | 第三排 (Row 2)，單幀尺寸 32x32px。支援方向旋轉及 4 幀行走動畫。備用為動態 SVG。 |
| `healer` | 治療兵 | 精靈圖 `PIC/EEMYAs.png` | 第四排 (Row 3)，單幀尺寸 32x32px。支援方向旋轉及 4 幀行走動畫。備用為動態 SVG。 |
| `stealth` | 隱形兵 | 動態 SVG | 透過 `EnemyGraphics.generateSVG()` 動態渲染，無外部圖檔。透明度隨隱形相位變化。 |
| `drone` | 雄蜂無人機 | 動態 SVG | 透過 `EnemyGraphics.generateSVG()` 動態渲染，無外部圖檔。 |
| `boss_berserker`| 狂暴魔王 | 精靈圖 `PIC/Berserks.png` | 第一排 (Row 0)，單幀尺寸 48x48px。0-1 幀為行走，2-3 幀為攻擊。不隨移動方向旋轉。 |
| `boss_carrier` | 母艦魔王 | 精靈圖 `PIC/Berserks.png` | 第二排 (Row 1)，單格尺寸 96x48px 特殊大圖。不隨移動方向旋轉。 |
| `skullsword` | 特攻骷髏劍 | 精靈圖 `PIC/Berserks.png` | 第三排 (Row 2)，單幀尺寸 48x48px。支援方向旋轉。 |
| `boss_armored` | 裝甲魔王 | 動態 SVG | 透過 `EnemyGraphics.generateSVG()` 動態渲染，無外部圖檔。本體會自動旋轉。 |
| `boss_teleporter`| 傳送魔王 | 動態 SVG | 透過 `EnemyGraphics.generateSVG()` 動態渲染，無外部圖檔。本體會自動旋轉。 |
| `tankbug` | 坦克蟲 | 專屬圖片 `PIC/TANKBUG2.png` | 獨立專屬圖檔。尺寸為 96x96px，包含兩幀高度 48px 的動畫。支援方向旋轉與行走動畫。 |

---

## 4. 各角色特殊能力詳細說明

### 1. 普通兵 (`normal`)
- **說明**：最基本且均衡的部隊，沒有任何特殊技能，單純沿著道路走向基地。

### 2. 偵察兵 (`scout`)
- **說明**：以極快的移動速度（倍率 1.8）和較小的體型（半徑 9）突破防線。適合消耗攻擊力大但攻速慢的防禦塔（如狙擊塔）的火力。

### 3. 坦克 (`tank`)
- **說明**：擁有高血量（倍率 2.5）但移動緩慢。在成功到達基地時，會對基地造成高達 100 點重創（一般小怪為 50 點）。

### 4. 治療兵 (`healer`)
- **說明**：**群體治療機制**。
  - 每 120 幀（約 2 秒）會在自身周圍 100 像素內進行群體回復。
  - 回復範圍內所有友軍最大生命值的 15%。

### 5. 隱形兵 (`stealth`)
- **說明**：**光學隱形能力**。
  - 它的透明度會隨時間呈正弦波規律變化（隱形相位）。
  - 在處於隱形狀態時，無法被「狙擊塔 (`sniper`)」與「電漿塔 (`plasma`)」鎖定。

### 6. 雄蜂無人機 (`drone`)
- **說明**：極低的生命值（倍率 0.3）但速度高達 2.2。被擊殺時，玩家獲得的金幣大幅減少（僅為正常公式的 20%）。

### 7. 狂暴魔王 (`boss_berserker`)
- **說明**：**垂死狂暴** & **遠程轟炸**。
  - 當生命值低於 50% 時，速度會隨著血量流失等比飆升，瀕死時最高可達原本速度的 2 倍。
  - 具備遠程攻擊力 (30)，射程 180，每 40 幀朝範圍內最近的防禦塔（或基地）發射子彈。

### 8. 母艦魔王 (`boss_carrier`)
- **說明**：**無人機孵化母艦**。
  - 自身血量固定為 1000 HP，且不具備直接攻擊能力。
  - 每隔 60 幀（約 1 秒），會在目前位置孵化出一隻「特攻骷髏劍 (`skullsword`)」小兵，發動自殺襲擊。

### 9. 特攻骷髏劍 (`skullsword`)
- **說明**：**直線自殺式撞擊**。
  - 由母艦孵化（或關卡配置產生），血量固定為 100 HP，擊殺無資金奖励。
  - **不遵循地圖道路移動**，而是走直線飛向最近的防禦塔（或基地）。
  - 當靠近目標時，會造成等同於其**當前剩餘生命值**的巨大傷害，並自我引爆毀滅。

### 10. 裝甲魔王 (`boss_armored`)
- **說明**：**狀態免疫護甲** & **重砲轟炸**。
  - 擁有全遊戲最高的基礎生命倍率 (12.0)。
  - **免疫投射物施加的狀態效果**：不受冰霜緩速、火焰燃燒持續傷害、毒液中毒持續傷害。
  - 具備極強的遠程攻擊力 (40)，射程 200，冷卻 60 幀。

### 11. 傳送魔王 (`boss_teleporter`)
- **說明**：**空間跳躍** & **遠程打擊**。
  - 每隔 240 幀（約 4 秒）會沿著當前的移動方向向前瞬間移動 100 像素，能輕易閃避防禦塔的集火或範圍陷阱。
  - 具備中等遠程攻擊力 (15)，射程 250，冷卻 35 幀。

### 12. 坦克蟲 (`tankbug`)
- **說明**：**前線防禦塔破壞者** & **極高撞擊傷害**。
  - 雖屬於小兵（Minion），但擁有高達 5.0 倍的生命倍率，且到達基地會造成 100 點重創。
  - 具備恐怖的近戰/中距攻擊力 (150)，且**每波攻擊力會額外成長 +10**（加上小怪共用成長，每波共增加 15 點攻擊力），專門用來摧毀玩家前線的防禦塔。
